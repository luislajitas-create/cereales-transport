# Opcional para desarrollo local: si no definís VITE_API_URL, el frontend ya apunta a
# http://localhost:3000/api/v1 por defecto (ver src/api/client.ts). Solo hace falta este
# archivo si necesitás que el frontend local apunte a otro backend.
VITE_API_URL=http://localhost:3000/api/v1
